## NOTES

On the mac, you need to run something like this

`dotnet user-secrets set "OpenAI:ApiKey" "sk-proj-Gjsasdf...IOIUO6I"`
to get the API key into your user secrets store.


`dotnet user-secrets init`
if you haven't already initialized user secrets for the project.